# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Virtual environment (recreate from requirements.txt) --------------------
.venv/
venv/

# --- Generated data (recreate by running data/genre_ordering.py) -------------
data/dataset.csv
data/genre_ordered_tracks.csv
data/buffer/

# --- Generated model artifacts (recreate by running retrainer/train_model.py)-
models/
# (model_v*.joblib, scaler_v*.joblib, metadata_v*.json, current.txt)

# --- MLflow local tracking store (the container uses a Docker volume) ---------
mlruns/
mlflow.db
mlartifacts/

# --- OS / editor cruft -------------------------------------------------------
.DS_Store
.vscode/
.idea/